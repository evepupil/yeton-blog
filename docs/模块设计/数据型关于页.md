# 数据型关于页

> **模块定位**：用可验证的公开数据和站点配置展示作者近期状态
>
> **对应代码**：`app/(zh)/about/`、`app/(en)/en/about/`、`features/about/`、`lib/about-status/`、`lib/weread/`、`functions/api/about-status.ts`、`scripts/sync-weread.ts`、`data/reading-status.json`
>
> **所属 M 里程碑**：[M10：数据型关于页](../roadmap.md#阶段-10数据型关于页)
>
> **当前状态**：已完成
>
> **最近更新时间**：2026-07-20

## 职责与边界

本模块负责关于页的个人资料、GitHub 贡献、AI Token 汇总、微信读书状态、技术雷达和数据来源说明。品牌、作者、头像、社交链接和外部服务地址继续由 `site.config.ts` 统一管理。

关于页只读取公开数据。TokenBoard 的费用、缓存细节和其他内部统计不会进入浏览器响应；Notion Token、微信读书 API Key 等私密凭据也不属于此模块的前端配置。

## 结构与数据流

```text
GitHub contribution HTML ─┐
                          ├─ Pages Function /api/about-status ─ AboutActivity
TokenBoard public JSON ───┘

WEREAD_API_KEY ─ GitHub Action ─ 官方 Agent Gateway
                                  │
                                  └─ data/reading-status.json + public/images/reading
                                                         │
                                                         └─ ReadingSection
site.config.ts ────────────── Hero + TechnologyRadar
```

- `AboutPageView` 是服务端页面组合层，输出 Hero、阅读、技术雷达和数据来源。
- `AboutActivity` 是页面里唯一需要浏览器请求的 Client Component，访问同源 `/api/about-status`。
- `loadAboutActivityStatus` 并行读取 GitHub 与 TokenBoard。每个来源单独超时和降级，一个失败不会清空另一个结果。
- Pages Function 返回公开缓存头，Cloudflare 可缓存 30 分钟，并在上游短时故障时继续使用旧响应。

## 关键决策

### 页面结构

正式页面沿用 `prototype/about.html` 已确认的白底、开放式分区和细分隔线。用户确认后删除 Hero 与活动区之间的深色“最近关注”横条。页面顺序固定为个人资料、最近 365 天、GitHub、TokenBoard、微信读书、技术雷达和数据来源。

### GitHub 贡献日历

`lib/about-status/github.ts` 使用 `parse5` 解析 GitHub 公开贡献页的结构化 HTML，读取每个日期的 `data-level`，并从关联的 `tool-tip` 提取精确贡献数。解析结果按日期排序，再计算活跃天数和总贡献数。页面结构缺失或活跃格子没有精确数量时直接判定该来源不可用，避免显示猜测值。

### TokenBoard 公开统计

TokenBoard 读取公共资料 JSON。Zod 只接收总 Token、今日、本月、来源分布和常用模型，输出对象重新逐字段组装。上游即使附带费用、缓存 Token 或其他字段，博客接口也会将它们丢弃。

当前公开接口没有最近 30 天逐日趋势，因此正式页面展示真实的来源比例和常用模型，没有沿用原型中的示意柱状图。

### 微信读书

`scripts/sync-weread.ts` 通过微信读书官方 Agent Gateway 请求本年度统计和书架，使用 `skill_version: 1.0.4`。同步只保留最近 3 本有阅读记录的公开书籍，`secret === 1` 的私密条目不会进入生成文件，API Key、bookId 和深度链接也不会写入仓库。

同步前先完整取得并校验两份接口数据，再在临时目录下载封面。接口失败、返回升级提示或文件替换失败时保留上一份 JSON 和封面；单张封面下载失败时只清空该书封面路径，由页面显示占位图。`WEREAD_API_KEY` 为空时脚本以成功状态跳过且不创建文件，因此新部署和普通构建不依赖这项私密配置。

### 技术雷达

技术名称和 Adopt、Trial、Assess 阶段统一配置在 `site.config.ts` 的 `profileStatus.technologyRadar`。SVG 只负责把配置映射到固定视觉位置，图例使用 HeroUI `Chip`，不在组件中重复维护技术清单。

## 当前实现

| 目录或文件                           | 作用                                 |
| ------------------------------------ | ------------------------------------ |
| `features/about/about-page.tsx`      | 服务端页面结构、个人资料和数据来源   |
| `features/about/about-activity.tsx`  | 客户端活动请求、热力图与 Token 分布  |
| `features/about/reading-section.tsx` | 微信读书数据与空状态                 |
| `features/about/tech-radar.tsx`      | 配置驱动的技术雷达                   |
| `lib/about-status/github.ts`         | GitHub HTML 解析                     |
| `lib/about-status/tokenboard.ts`     | TokenBoard JSON 校验与字段裁剪       |
| `lib/about-status/service.ts`        | 并行请求、超时和单来源降级           |
| `lib/weread/`                        | 官方接口校验、公开字段转换和原子写入 |
| `scripts/sync-weread.ts`             | 本地与 CI 同步入口                   |
| `.github/workflows/sync-weread.yml`  | 每日同步、定向提交和自动部署触发     |
| `functions/api/about-status.ts`      | 同源公开接口与 Cloudflare 缓存策略   |

## 验证方式

- `tests/unit/about-status.test.ts` 覆盖 GitHub 数量与等级解析、无贡献日、空页面、缺失 tooltip、TokenBoard 字段裁剪和单来源失败。
- `tests/unit/weread-sync.test.ts` 覆盖年度统计换算、私密过滤、最近书籍排序、接口错误与升级提示、缺 Key 跳过、旧快照保护和封面失败降级。
- `tests/unit/site-config.test.ts` 覆盖 GitHub 用户名、TokenBoard 公共 URL 和技术雷达配置。
- Playwright 使用受控 `/api/about-status` 响应验证中英文内容、热力格、Token 数据、微信读书空状态、技术雷达和移动端无溢出。
- 视觉验收使用构建后的静态产物，检查 `1440 x 1000` 与 `390 x 844`，不启动开发服务器。

## 待扩展项

- 微信读书官方 Skill 版本升级时，需要先核对接口文档和字段说明，再更新同步脚本中的 `skill_version`。
- 当前只展示电子书；需要把有声书纳入页面时，再按官方 `albums` 字段增加独立映射。
- TokenBoard 公共 API 提供逐日趋势后，可在保持现有摘要的前提下增加时间序列。
- GitHub 页面结构变化时，需要根据受控失败日志更新解析器和夹具。

## 改动历史

### 2026-07-20

- 建立数据型关于页模块，接入 GitHub 与 TokenBoard 公开数据。
- 增加微信读书本地数据契约、技术雷达配置、中英文页面和响应式样式。
- 按用户确认删除原型和正式页面中的深色“最近关注”横条。
- 功能提交 `b14718b` 通过格式、lint、严格类型、134 条单测、6 条原型测试、生产构建、21 条 Playwright 和静态产物检查，M10 验收完成。
- 接入微信读书官方 Agent Gateway、独立每日 Action 和失败保护；缺少 `WEREAD_API_KEY` 时成功跳过并保留页面空状态。
- 本次扩展通过格式、lint、严格类型、142 条单测、6 条原型测试、生产构建、21 条 Playwright 和静态产物检查。
