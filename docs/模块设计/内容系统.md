# 内容系统

## 设计

### 职责

内容系统负责读取仓库内的文章和图书，检查 frontmatter、目录位置、slug、译文关联和图片路径，再生成页面可直接使用的正文信息、阅读数据、标签统计和归档分组。它只处理构建期数据，不负责正文渲染、分页界面、搜索交互和在线内容管理。

### 目录结构

| 路径                                   | 用途                                   |
| -------------------------------------- | -------------------------------------- |
| `content/posts/{zh,en}/`               | 中英文文章源文件                       |
| `content/books/{zh,en}/`               | 中英文图书源文件                       |
| `lib/content/schema.ts`                | 文章和图书 frontmatter 的 Zod 严格结构 |
| `lib/content/reader.ts`                | 文件发现、读取、校验和数据组装         |
| `lib/content/markdown.ts`              | Markdown 纯文本和二、三级目录提取      |
| `lib/content/metrics.ts`               | 中英文字数和阅读时间计算               |
| `lib/content/queries.ts`               | 发布过滤、排序、归档、标签和译文查询   |
| `lib/content/legacy-migration.ts`      | 旧站文章字段、摘要与图片路径转换       |
| `lib/content/legacy-book-migration.ts` | 旧站目录式图书的章节合并与链接转换     |
| `lib/content/types.ts`                 | 页面层消费的只读内容类型               |
| `scripts/migrate-legacy-content.ts`    | 可重复执行的旧站文章迁移入口           |
| `scripts/migrate-legacy-books.ts`      | 可重复执行的旧站图书迁移入口           |
| `scripts/validate-content.ts`          | 命令行内容检查入口                     |
| `tests/unit/content-*.test.ts`         | 内容规则单元测试                       |

### 关键决策

1. 内容保存在仓库内，构建时一次性读取。文章、标签、归档和图书页面因此可以完整静态导出。
2. 文章和图书都使用 Markdown/MDX 文件。frontmatter 只接受 schema 声明的字段，拼错字段会立即失败。
3. 文件名就是 slug，必须使用不含空格的小写 kebab-case，可以包含中文等 Unicode 字母。中文内容放在 `zh/`，英文内容放在 `en/`，目录与 `locale` 不一致会失败。
4. `translationKey` 只在确实存在译文时填写。同一个 key 必须连接两种语言，并且每种语言只能出现一次。
5. 图书正文中的二级、三级标题就是章节目录。文章使用同一套标题分析生成文内目录。
6. 内容文件并行读取；文章和图书也在校验脚本中并行加载，避免构建阶段串行等待。
7. 页面层只使用 `lib/content` 返回的数据。草稿过滤、置顶、日期排序和译文查找不在页面里重复实现。
8. Notion 生成的文章使用相同 schema，并额外带 `source: "notion"` 与 `notionPageId`。两个字段必须同时存在，手写文章无需填写。
9. 正文解析和渲染都启用 GFM，旧文章中的表格、任务列表、删除线和自动链接保持可读。
10. 图书可选保存作者、译者、出版和更新时间。目录式旧书合并后，以二级标题表示章节、三级及更深标题表示章内结构。

## 改动历史

### 2026-07-14

- 建立文章与图书的 Zod 严格 frontmatter 结构。
- 实现递归并行读取、slug 和语言目录校验、重复检查、译文配对与本地图片检查。
- 实现 Markdown 目录提取、搜索可用纯文本、中文字符与英文单词统计、阅读时间计算。
- 实现发布内容排序、年份归档、标签统计、图书排序和译文查询。
- 迁移 7 篇文章、3 本图书及对应本地图片，替换原型演示数据。
- 将 `pnpm content:check` 接入 `pnpm build`，让无效内容阻止静态构建。
- 阶段 3 增加构建期缓存仓库、轻量文章摘要、已发布文章查找和前后篇查询，页面不再重复读取或整理内容。

### 2026-07-15

- 阶段 4 增加按标签查询、已发布图书查找，以及文章和图书的目标语言译文查询。
- 增加构建期内容语言路由表，客户端只接收路径映射，不接收正文数据。
- 为图书进度的整数和 `0–100` 范围补充独立单测。
- 阶段 5 将已发布文章的标题、摘要、标签和 `plainText` 写入分语言搜索索引，并复用发布过滤规则生成 RSS 与 sitemap。
- 阶段 7 为 Notion 生成内容增加来源与页面 ID 校验；同步后的 Markdown 继续经过相同 reader、搜索、RSS 和 sitemap 流程。
- 旧站迁移允许保留小写 Unicode slug 和 translation key，中文文章可以继续使用原公开 URL。
- 新增可重复执行的旧站迁移器，将 notion-fuwari 的 33 篇文章和 31 张实际引用图片迁入当前内容系统，替换 7 篇演示文章。
- 迁移器将 `lang`、日期、摘要、封面和 Notion 来源字段转换为当前严格 schema；仅保留真实成对的翻译键，并在清理目标目录前校验路径和全部源图片。
- 正文图片统一迁到 `public/images/posts/`，Markdown 中的引用通过 AST 定位后改写，不会误改代码块中的示例文本。
- 增加 GFM 解析与正文图片、表格、任务列表样式，覆盖旧站真实文章使用的 Markdown 语法。
- 迁移时将正文一级标题降为二级，保证文章页面只保留文章标题这一个 `h1`；同时清理旧 Markdown 行尾空白。
- 新增目录式图书迁移器，将 notion-fuwari 的 3 本真实图书和 26 个章节合并为当前内容系统的 3 个 Markdown 文件，并删除演示图书。
- 图书迁移保留标题、摘要、标签、状态、作者、译者和日期；章节链接改为单页锚点，代码块中的示例文本不会被误改。
- 图书顺序使用显式配置，避免 Windows 和 Linux 对中文目录名的排序差异改变线上展示。

## 实现细节

### 读取流程

1. `loadArticles` 或 `loadBooks` 递归列出 `.md`、`.mdx` 文件并按路径排序。
2. `gray-matter` 拆分 frontmatter 和正文，Zod 检查字段类型、必填项、日期范围、标签重复和进度范围。
3. reader 检查文件名 slug、`locale` 所在目录、译文 key 唯一性和文章图片是否存在。
4. Markdown AST 提取纯文本与二、三级标题。相同标题使用稳定且唯一的锚点。
5. 文章纯文本按每分钟 300 个中文字符、200 个英文单词估算阅读时间，最少显示 1 分钟。
6. reader 返回整理后的只读对象；查询函数再按语言和草稿状态生成发布清单。

### 旧站迁移流程

1. `pnpm content:migrate-legacy -- --source <旧仓库> --replace` 读取旧仓库 `src/content/posts`。
2. 先统计双语 `translationKey`，只有同时存在中英文的键才写入新 frontmatter；单语文章省略该字段。
3. 摘要转为纯文本并限制在 240 字符内，日期固定输出为 `YYYY-MM-DD` 字符串，旧 `category` 等未声明字段不进入新文件。
4. 正文和封面引用的旧资源路径改为 `/images/posts/...`，只复制实际引用的图片。
5. 目标目录和全部源图片校验通过后才替换 `content/posts` 与 `public/images/posts`，最后继续执行常规内容校验。

### 旧站图书迁移流程

1. `pnpm content:migrate-legacy-books -- --source <旧仓库> --replace` 读取旧仓库 `src/content/books` 下的目录式图书。
2. 每本书先读取 `index.md` 元数据和导读，再按文件名顺序读取非草稿章节；所有源文件转换完成后才替换 `content/books`。
3. 迁移器为每个章节插入二级标题，并按该章节原有的最浅标题计算下移层级，保留标题关系且保证页面只有一个一级标题。
4. Markdown AST 只改写真正的旧章节链接和 `5.10.2` 这类小节编号链接，将它们指向合并后页面的稳定锚点，不触碰代码块；源书中找不到对应标题的错误编号会保留可读文字并移除死链。
5. 旧 `completed` 状态转换为 `complete` 和 `100` 进度；三本单语中文图书不生成虚假的英文译文关系。
6. 写入前使用仓库同版本 Prettier 和 `proseWrap: preserve` 整理 Markdown，保留原段落换行并保证重复迁移后可以直接通过格式门禁。

### 排序与查询

- 文章先按 `pinned` 置顶，再按 `published` 从新到旧排序。
- 图书先排除草稿并按语言筛选，再按 `order` 从小到大排序。
- 年份归档沿用文章顺序，标签统计按数量降序、名称升序排列。
- 译文只通过相同 `translationKey` 和目标语言匹配，并排除草稿，缺少译文时返回 `null`。
- 列表和首页使用 `ArticlePreview`，不会把正文、纯文本、目录和源文件路径传给客户端组件。
- `plainText` 与标题、摘要和标签一起写入 MiniSearch 索引；正文只存在于构建期索引输入，不会作为页面属性传给客户端组件。

### 异常处理

所有可预期的内容问题都抛出 `ContentValidationError`，消息以源文件路径开头，再说明具体字段或规则。`pnpm content:check` 捕获错误后返回非零退出码，`pnpm build` 因此会停止。

### 测试方法

- reader 测试在系统临时目录生成空内容、单篇内容和异常内容，覆盖日期、图片、译文、重复 slug 与语言目录。
- 纯函数测试覆盖目录锚点、纯文本、字数、阅读时间、发布筛选、置顶排序、归档、标签、图书顺序和译文映射。
- 完整 `pnpm content:check` 校验仓库中的全部真实内容和图片引用。

## 当前限制

- 当前正文只渲染安全 Markdown，不执行原始 HTML 或任意 MDX React 组件。
- 作者资料来自根目录 `site.config.ts`，首版只支持一位站点作者。
- Notion 只作为离线编辑源。GitHub Action 把内容同步成本地 Markdown 和图片后提交到仓库，网页访问时不会请求 Notion。
- 旧书目前合并为单页长文，章节数量较多时正文 HTML 会明显大于普通文章；后续若需要逐章阅读，可在保持现有公开图书地址的前提下增加章节子路由。
