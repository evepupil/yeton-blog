# 评论系统

> **模块定位**：为中英文文章提供按需加载的 GitHub Discussions 评论入口
>
> **对应代码**：`site.config.ts`、`lib/giscus/`、`features/comments/`、`features/posts/article-page.tsx`
>
> **所属 M 里程碑**：[M7：评论、同步与统计](../roadmap.md#阶段-7评论同步与统计)
>
> **当前状态**：进行中
>
> **最近更新时间**：2026-07-16

## 设计

### 职责

评论系统负责在中英文文章末尾提供 Giscus 讨论入口，处理公开配置校验、按需加载、明暗主题联动、超时、脚本失败和重试。正文、前后篇和站内导航不依赖 Giscus，评论服务不可用时文章仍可完整阅读。

### 目录结构

| 路径                                    | 用途                            |
| --------------------------------------- | ------------------------------- |
| `site.config.ts`                        | 评论开关和 Giscus 公开参数      |
| `lib/giscus/config.ts`                  | 启用配置的完整性和格式校验      |
| `features/comments/giscus-comments.tsx` | 客户端脚本加载、状态和主题消息  |
| `features/posts/article-page.tsx`       | 将评论区接到中英文文章阅读流    |
| `features/posts/posts.css`              | 评论区桌面和手机样式            |
| `public/_headers`                       | 放行 Giscus 脚本、连接和 iframe |
| `tests/unit/giscus-config.test.ts`      | 配置缺失、完整和异常组合测试    |
| `tests/e2e/home.spec.ts`                | 评论加载、主题联动和失败降级    |

### 关键决策

1. Giscus 使用 `pathname` 映射和严格模式。中文与英文文章路径不同，因此各自拥有独立讨论。
2. 评论采用明确的“加载评论”按钮。点击前页面不会连接 `giscus.app`，减少首屏资源和无意的第三方请求。
3. 仓库、仓库 ID、分类和分类 ID 都是公开值，统一保存在 `site.config.ts`。`enabled` 关闭时不加载评论；开启后字段不完整会让构建失败。
4. `next-themes` 提供当前明暗主题。iframe 首次创建时使用当前主题，后续切换通过 `{ giscus: { setConfig: { theme } } }` 消息更新。
5. 脚本错误或 15 秒内没有可用 iframe 时进入失败状态。失败状态保留文章内容，并提供重新加载按钮。
6. CSP 只为 `https://giscus.app` 增加脚本、连接和 iframe权限，没有扩大图片、表单或其他第三方来源。

## 改动历史

### 2026-07-16

- 配置 `evepupil/yeton-blog` 的 `General` Discussions 分类和 Giscus 分类 ID，打开线上评论开关。
- 评论输入框使用 Giscus 生成配置中的底部位置，保留项目现有的双语、主题联动和点击后加载行为。
- 更新真实公共配置单测，继续由 Playwright 隔离模拟 Giscus 成功和失败状态。

### 2026-07-15

- 增加 Giscus 配置 schema 和中英文评论状态文案。
- 增加点击加载、iframe 就绪、主题同步、超时、失败与重试流程。
- 将评论区接到文章前后篇之后，并补充手机布局。
- 扩展 CSP 和静态产物检查。
- 增加配置单测和两条 Playwright 评论流程。
- 将 Giscus 公开参数迁入统一站点配置，移除四个 Cloudflare Pages 构建变量。

## 实现细节

### 公开配置

| `site.config.ts` 字段              | 当前值或来源                                 |
| ---------------------------------- | -------------------------------------------- |
| `integrations.comments.repo`       | `evepupil/yeton-blog`                        |
| `integrations.comments.repoId`     | `R_kgDOTY-rvQ`，由 GitHub 公开 REST API 确认 |
| `integrations.comments.category`   | `General`                                    |
| `integrations.comments.categoryId` | `DIC_kwDOTY-rvc4DBSku`                       |
| `integrations.comments.enabled`    | `true`                                       |

配置值会出现在页面和网络请求中，不应当存放 Token、密码或其他私密数据。

### GitHub 前置设置

1. 仓库所有者已为 `evepupil/yeton-blog` 开启 Discussions。
2. Giscus GitHub App 已获得该仓库权限。
3. Giscus 使用 `General` 分类，仓库 ID 和分类 ID 已写入 `site.config.ts`。
4. 部署后打开一篇文章，加载评论并完成一次测试讨论，再验证明暗主题和登录流程。

### 客户端状态

- `disabled`：配置开关关闭，显示评论暂未开放。
- `idle`：配置完整，等待读者点击加载。
- `loading`：脚本已插入，等待 Giscus iframe 完成加载。
- `ready`：iframe 可用，主题变化通过消息同步。
- `error`：脚本失败或超时，显示说明和重试按钮。

组件卸载或文章切换时会清理超时器、DOM 观察器、脚本和 iframe，避免把上一页讨论带到下一篇文章。

### 隐私边界

点击加载前只渲染本站 HTML、CSS 和 JavaScript。点击后浏览器会连接 Giscus 和 GitHub，第三方可能接收 IP、User-Agent、GitHub 登录状态以及评论交互数据。本站不代理、不复制评论内容，也不额外保存 GitHub 身份信息。

### 测试方法

- 配置单测覆盖关闭、完整启用、字段错误和 Playwright 测试数据。
- Playwright 拦截 `giscus.app/client.js`，用同源测试 iframe 接收主题消息，不依赖真实 GitHub。
- 失败测试在 `390 x 844` 视口中断脚本请求，确认错误提示和重试按钮出现，同时文章标题仍可见。
- `pnpm build` 检查 `_headers` 包含 Giscus 所需的 `script-src` 与 `frame-src`。

## 当前限制

- Giscus 公共配置和站点开关已经完成；生产部署后仍需验证真实 iframe 加载。
- Giscus 的登录、发言、反应和管理流程需要使用 GitHub 账号人工确认。
